export const Button = ({ colorScheme, variant, size }) => {
      return (
            <button style={{ size:"md",
            // height:'48px',
            // width:'200px',
            border:'2px',
            borderColor:'green'}}>
                  Button
            </button>
      );
};
